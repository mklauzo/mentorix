# ============================================================
# Mentorix AI Agent – Environment Variables
# Copy to .env and fill in values
# ============================================================

# === Domain ===
DOMAIN=yourdomain.pl
ACME_EMAIL=admin@yourdomain.pl

# === Security ===
SECRET_KEY=change_me_to_random_64_char_hex_string
JWT_SECRET_KEY=change_me_to_another_random_64_char_hex_string
IP_HASH_SALT=change_me_to_random_salt_for_ip_hashing

# === PostgreSQL ===
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=mentorix
POSTGRES_USER=mentorix_user
POSTGRES_PASSWORD=change_me_strong_password

# === Redis ===
REDIS_URL=redis://redis:6379/0

# === OpenAI (opcjonalne – globalne fallback dla embeddings) ===
# Klucz API można ustawić per-profil w panelu admina (Edytuj profil → Klucz API).
# Ustaw tutaj tylko jeśli chcesz globalnego fallbacku dla wszystkich profili.
# OPENAI_API_KEY=sk-...

# === Ollama (local LLM – free, runs in Docker) ===
# URL inside Docker network – do not change unless running Ollama externally.
OLLAMA_URL=http://ollama:11434
# After first start, pull models you want to use, e.g.:
#   docker compose exec ollama ollama pull llama3.2
#   docker compose exec ollama ollama pull mistral
#   docker compose exec ollama ollama pull gemma3
# Then set llm_model in a profile to e.g. "ollama:llama3.2"

# === App Settings ===
ENVIRONMENT=production
LOG_LEVEL=INFO
UPLOAD_MAX_SIZE_MB=25
UPLOAD_DIR=/uploads

# === Traefik ===
TRAEFIK_DASHBOARD_USER=admin
# Generate: htpasswd -nb admin 'YourPassword'
# IMPORTANT: every $ in the hash must be doubled to $$ (Docker Compose escaping)
# Example output from htpasswd:  admin:$apr1$xxxx$yyyy
# What to put here:              admin:$$apr1$$xxxx$$yyyy
TRAEFIK_DASHBOARD_PASSWORD_HASH=admin:$$apr1$$change$$me000000000000000000

# === Monitoring (optional) ===
GRAFANA_ADMIN_PASSWORD=change_me_grafana_password

# === Celery ===
CELERY_BROKER_URL=redis://redis:6379/1
CELERY_RESULT_BACKEND=redis://redis:6379/2

# === CORS ===
# Admin API allowed origins (comma-separated)
ADMIN_CORS_ORIGINS=https://yourdomain.pl
